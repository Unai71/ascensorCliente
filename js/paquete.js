export default class Paquete {
    constructor(peso, dimensiones, destino) {
        this.peso = peso; // El peso del paquete en kilogramos.
        this.dimensiones = dimensiones; // Un objeto o array con las dimensiones (ancho y alto) del paquete.
        this.destino = destino; // El piso de destino del paquete.
    }
}
